### Introduction

This contains the first sample exercise from the book. A simple tree walk interpreter for `minijava`.


Compile/build using `make` and run using `java -classpath out/ Interp` or `make run`.